# [빨모쌤과 함께하는 영어공부] 개발 기획서

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 빨모쌤과 함께하는 영어공부 |
| 목표 | Notion에 정리된 영어 표현/교재 내용을 반복 학습·연습할 수 있는 개인용 웹앱 제작 |
| 사용 환경 | 모바일 웹 (반응형, 세로 화면 기준 최적화) |
| 데이터 소스 | Notion 데이터베이스 (API 연동) |
| 사용자 | 1인 사용 (본인 학습용) |

> ⚠️ **보안 주의**: Notion API 키는 절대 코드에 직접 작성하거나 문서·깃허브에 올리지 않습니다. `.env` 파일에 저장하고 `.gitignore`에 등록합니다. (이번에 채팅에 노출된 키는 노션 설정에서 재발급 필요)

```
# .env 예시 (실제 값은 절대 여기 적지 않고 로컬 .env 파일에만 둔다)
NOTION_API_KEY=your_notion_api_key_here
NOTION_DB_ID_YOUTUBE=your_youtube_db_id_here
NOTION_DB_ID_BOOK=your_book_db_id_here
```

---

## 2. 전체 시스템 구조

```
[Notion DB] --(Notion API, 주기적 동기화)--> [Flask 서버] --(JSON 캐시 저장)--> [data/*.json]
                                                     |
                                                     v
                                        [Jinja2 템플릿 렌더링]
                                                     |
                                                     v
                                        [모바일 웹 브라우저 - HTML/CSS/JS]
```

- Notion API를 매 요청마다 호출하지 않고, **동기화 버튼 또는 배치 스크립트**로 `data/` 폴더의 JSON 파일을 갱신하는 구조를 권장합니다. (속도, API 호출 제한 대응)
- 프론트엔드는 순수 JS로 문장 넘기기·TTS 재생·속도 조절 등 인터랙션을 처리합니다.

---

## 3. 데이터 모델 설계

### 3-1. 유튜브 학습 DB (home > 영어노트 > 유튜브database)

로컬 캐시 파일: `data/youtube_lessons.json`

```json
{
  "2025-07-31": {
    "youtube_url": "https://youtube.com/watch?v=xxxx",
    "keywords": [
      { "id": 1, "en": "How have you been?", "ko": "어떻게 지냈어?" },
      { "id": 2, "en": "I've been pretty busy lately.", "ko": "요즘 꽤 바빴어." }
    ],
    "my_sentences": [
      { "id": 1, "en": "I finally finished the project.", "ko": "드디어 프로젝트를 끝냈어." }
    ]
  }
}
```

- `keywords`: 대화 형식일 경우 배열 순서가 곧 대화 순서입니다. (화자 구분이 필요하면 `speaker` 필드 추가 검토)
- 날짜는 Notion에 저장된 값 기준이며, 화면에는 `YY/MM/DD`로 표시합니다.

### 3-2. 책 학습 DB (home > 책내용상세정리 > 빨모쌤의라이브영어회화)

로컬 캐시 파일: `data/book_units.json`

```json
{
  "UNIT16": {
    "title": "UNIT 16. 제목",
    "sentences": [
      { "id": 1, "en": "I really **appreciate** your help.", "ko": "정말 네 도움에 감사해.", "bold_words": ["appreciate"] }
    ]
  }
}
```

- Notion의 bold 서식은 `bold_words` 배열이나 `**텍스트**` 마크업으로 변환해 캐시에 저장하고, 프론트에서 `<strong>`으로 렌더링합니다.
- UNIT 16 ~ UNIT 75까지 동일한 구조를 반복합니다.

---

## 4. 화면별 기능 명세

### 4-1. 홈/날짜선택 화면
- 학습한 날짜 목록을 `YY/MM/DD` 형식으로 캘린더 또는 리스트로 표시
- 날짜를 탭하면 해당 날짜의 학습 콘텐츠(유튜브 URL + 키워드 + 나만의 문장)로 이동
- 유튜브 URL 아이콘을 탭하면 새 탭으로 해당 영상 바로 이동

### 4-2. 학습 키워드 화면
- Notion에 정리된 문장이 **순차적으로** 표시됨 (대화형이면 대화 순서대로)
- 문장별 한국어 · 영어 동시 표시, 한국어는 **on/off 토글**로 숨김 가능
- 하단 컨트롤 바
  - 속도 조절 버튼: 0.5배속 ~ 2배속 (TTS 재생 속도)
  - 화면 탭 1회 → 다음 문장으로 자동 이동
  - ◀ ▶ 이동 버튼 → 이전/다음 문장 수동 이동
  - 목차 버튼 → 전체 문장 리스트를 한 화면에 표시, 탭하면 해당 문장으로 바로 이동

### 4-3. 연습하기 화면
- 해당 날짜의 모든 한글·영어 문장을 **목록형으로 한눈에** 표시 (학습 키워드 화면과 달리 스크롤 뷰)
- 문장을 탭하면 그 문장만 TTS로 읽어줌

### 4-4. 나만의 문장 화면
- 학습 키워드 화면과 **동일한 UI/동일한 인터랙션 구조** 재사용 (컴포넌트 공용화 권장)
- 데이터만 `keywords` 대신 `my_sentences`를 사용

### 4-5. 책공부하기 화면 (빨모쌤의 라이브 영어회화)
- UNIT 16 ~ UNIT 75 목차 리스트 화면 → UNIT 탭 시 해당 UNIT 상세로 이동
- UNIT 상세: 문장 순차 학습, 탭하면 다음 문장, 한글/영어 on/off 지원
- Notion에서 bold 처리된 단어는 화면에서도 **굵게** 그대로 표시

---

## 5. 공통 UI 컴포넌트 (재사용 설계)

같은 인터랙션이 여러 화면에서 반복되므로, 아래를 공통 JS 컴포넌트로 분리하는 것을 권장합니다.

- `SentencePlayer`: 문장 순차 재생/이동/목차 기능 (학습 키워드, 나만의 문장, 책공부 UNIT 상세에서 공용)
- `TTSControl`: 재생/속도 조절 (Web Speech API 활용 검토)
- `KoEnToggle`: 한국어 표시 on/off
- `DatePicker`: 학습 날짜 선택 및 학습완료 날짜 하이라이트

---

## 6. Notion 연동 설계

1. 개발 초기엔 Notion API를 직접 호출해 구조 확인 → 이후 동기화 스크립트(`sync_notion.py`)로 전환
2. 동기화 스크립트가 Notion DB를 읽어 `data/*.json`으로 저장
3. Flask 앱은 항상 로컬 JSON만 읽어서 서빙 (API 호출 최소화, 응답 속도 확보)
4. 동기화 주기: 수동 실행 버튼(관리자 페이지 또는 CLI 명령) 또는 하루 1회 스케줄

---

## 7. 기술 스택 및 폴더 구조

```
- 언어: Python 3
- 웹 프레임워크: Flask (Jinja2 템플릿)
- 프론트엔드: HTML, CSS, JavaScript (Vanilla JS 권장, 러닝커브 최소화)
- 데이터 저장: JSON 파일 (data/*.json)
- 포트: 5000 (충돌 시 5001)
```

```
project/
├── app.py                  # Flask 진입점
├── sync_notion.py          # Notion → JSON 동기화 스크립트
├── .env                    # API 키 (git 제외)
├── .gitignore
├── data/
│   ├── youtube_lessons.json
│   └── book_units.json
├── templates/
│   ├── index.html           # 날짜선택
│   ├── keywords.html        # 학습 키워드
│   ├── practice.html        # 연습하기
│   ├── my_sentences.html    # 나만의 문장
│   ├── book_units.html      # UNIT 목차
│   └── book_unit_detail.html
└── static/
    ├── css/
    └── js/
        ├── sentence-player.js
        ├── tts-control.js
        └── ko-en-toggle.js
```

---

## 8. 개발 우선순위 (MVP → 확장)

1. **MVP 1단계**: Notion 동기화 스크립트 + 날짜선택 화면 + 학습 키워드 화면 (탭 넘기기, 한/영 토글)
2. **MVP 2단계**: 속도 조절, 목차보기, 연습하기 화면
3. **확장 1**: 나만의 문장 화면 (키워드 화면 컴포넌트 재사용)
4. **확장 2**: 책공부하기 (UNIT 16~75), bold 표시 처리

---

## 9. 추후 고려사항

- Web Speech API의 브라우저별 TTS 음질 편차 → 필요시 외부 TTS(예: 클라우드 TTS)로 교체 검토
- 학습 진도/완료 여부를 로컬에 저장할지, 별도 JSON으로 관리할지 결정 필요
- 모바일 홈 화면에 추가(PWA) 여부 결정 시 manifest.json 추가 검토
